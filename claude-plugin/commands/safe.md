---
description: Switch Frugal Tokens to Safe Mode (observe-only, no intervention)
---

Run `python "${CLAUDE_PLUGIN_ROOT}/bin/frugal.py" safe` and confirm to the
user: Frugal is now in shadow profile — measurement and budget warnings only,
zero workflow intervention. Claude Code behavior is completely unchanged.
