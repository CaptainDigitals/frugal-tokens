---
description: Run Frugal Tokens health checks (runtime, database, hooks, status line, telemetry)
---

Run `python "${CLAUDE_PLUGIN_ROOT}/bin/frugal.py" doctor` and present the
result. For any failed check, offer the fix the hint suggests — apply
configuration changes only with the user's approval.
